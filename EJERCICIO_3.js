for (let i = 1; i <= 10; i++) {
  let numero = Number(prompt("Ingresa el número " + i + ":"));

  if (numero > 50) {
    console.log("El número " + numero + " es mayor que 50.");
  }
}

//prompt pide cada número (convertido a número con Number)//

//Si el valor ingresado es mayor a 50, se muestra en consola//

//Si no, no se muestra nada//